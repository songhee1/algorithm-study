#include<bits/stdc++.h>
using namespace std;
int N, num[1005];
int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    cin>>N;
    for(int i=0;i<N;i++){
        cin>>num[i];
    }
    int min=num[0];
    for(int i=i;i<N;i++){
        if(min>num[i])
    }
    
}